# Runtime dependencies for the deployed Code Nexus app (backend/app.py).
Flask>=3.0,<4.0
gunicorn>=22,<24
# Powers POST /api/ml/compare. The import is lazy: if this build is too large
# for the host (e.g. Vercel Hobby's 250 MB Python bundle), remove this line and
# redeploy — every other endpoint keeps working and /api/ml/compare returns 503.
scikit-learn>=1.5,<2
